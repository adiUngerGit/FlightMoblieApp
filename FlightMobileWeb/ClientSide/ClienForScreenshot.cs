/*using Microsoft.AspNetCore.Mvc;
using Microsoft.Extensions.Options;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Threading.Tasks;

namespace FlightMobileWeb.ClientSide
{
    public class ClientScreenshot
    {
        private HttpClient client;
        //private readonly Config _myConfiguration;

        public ClientScreenshot()
        {
            client = new HttpClient();
          //  _myConfiguration = myConfiguration.Value;
        }
        public async Task<byte[]> getScreenshot()
        {
            try
            {
                //string[] conf = _myConfiguration.Urlhttp.Split(":");
                //Task<byte[]> array = client.GetByteArrayAsync("http://" + conf[0] + ":" + conf[1] + "/screenshot");
                //return await array;
                return null;
            }
            catch
            {
                return null;
            }
        }
    }
}
*/